import {SUPABASE_URL,SUPABASE_KEY} from './firebase.js';const tg=window.Telegram?.WebApp;tg?.expand();const u=tg?.initDataUnsafe?.user||{id:0};const h={apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`};fetch(`${SUPABASE_URL}/rest/v1/users?id=eq.${u.id}&select=approved,mining,plan`,{headers:h}).then(r=>r.json()).then(d=>{const x=d[0];document.getElementById('state').textContent=x?.approved?(x.mining?'Mining is active.':'Approved — press Start Mining on Home.'):'Waiting for admin approval.'}).catch(()=>document.getElementById('state').textContent='Database error');
