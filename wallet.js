import {SUPABASE_URL,SUPABASE_KEY} from './firebase.js';const tg=window.Telegram?.WebApp;tg?.expand();const u=tg?.initDataUnsafe?.user||{id:0};const h={apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'};const $=x=>document.getElementById(x);$('withdraw').onclick=async()=>{try{const amount=Number($('amount').value);const address=$('address').value.trim();if(!address||amount<=0)throw Error('Enter a valid address and amount');await fetch(`${SUPABASE_URL}/rest/v1/withdrawals`,{method:'POST',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({user_id:Number(u.id),amount,address,status:'pending'})});$('msg').textContent='Withdrawal request sent to admin.';$('msg').className='ok'}catch(e){$('msg').textContent=e.message;$('msg').className='bad'}};
